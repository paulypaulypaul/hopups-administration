'use strict';

/**
 * @ngdoc function
 * @name adminApp.controller:TurdCtrl
 * @description
 * # TurdCtrl
 * Controller of the adminApp
 */
angular.module('adminApp')
  .controller('TurdCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];

    ['turd'];
    if (donkey){
      var sep = true;
    }

  });
